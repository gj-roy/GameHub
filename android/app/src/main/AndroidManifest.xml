<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ca.on.hojat.gamenews">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--    Only Needed for React Native's debug window which shows up during debug
     time of the app (Shouldn't be included in release build). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!--    This permission is only for the sake of older API when we
    want to download something to external storage -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:name=".GameHubApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/GameNewsThemeSplash"
        android:usesCleartextTraffic="true"
        tools:ignore="Instantiatable,UnusedAttribute"
        tools:targetApi="28">

        <!-- See this https://stackoverflow.com/a/68899786/7015881 why tools:ignore is needed above -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:windowSoftInputMode="adjustPan"
            tools:ignore="LockedOrientationActivity" />

        <activity android:name="com.facebook.react.devsupport.DevSettingsActivity" />

        <!--        This is the root activity to the React Native part of the app. -->
        <activity
            android:name=".ReactActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".core.downloader.DownloadCompletionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
