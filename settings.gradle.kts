rootProject.name = "Gamedge"

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}

include(":app")
include(":common-data")
include(":database")
include(":shared")
