rootProject.name = "GameNews"

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}

include(":app")
include(":shared")
include(":core")
include(":data")
include(":common_ui")
